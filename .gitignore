# Nix
# keep-sorted start
.direnv/
/result
/result-*
# keep-sorted end

# Rust
# keep-sorted start
/target/
# keep-sorted end

# Tooling
# keep-sorted start
# Claude Code's scratch worktrees. Left untracked they abort `cog bump`, which
# refuses to run with anything uncommitted.
.claude/
lefthook-local.yml
# keep-sorted end

# Local secrets/config — copy .env.example to .env
# keep-sorted start
.env
# keep-sorted end

